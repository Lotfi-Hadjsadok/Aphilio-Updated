/** Fallback when a scraped section or document has no heading. */
export const DEFAULT_SECTION_TITLE = "Untitled section";

/** Single-group label when only a flat URL list is available (legacy payloads). */
export const LEGACY_FLAT_REFERENCE_SECTION_TITLE = "Reference images";
